/** Every path in the app. */
export const routes = {
  login: '/prijava',
  admin: '/admin',
  /** Not linked from the UI — the bare board for a projector. */
  standings: '/rang-lista',
} as const;
